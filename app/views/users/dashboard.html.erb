<% if @current_user.admin? %>
  <%= render partial: "users/admin", locals: {reserved_books: @reserved_books, waitlist: @waitlist, requests: @requests, users: @users, books: @books} %>
<% else %>
  <%= render partial: "users/student", locals: {waitlist: @student_waitlist, books: @books} %>
<% end %>
