<% if @current_user.admin? %>
  <%= render partial: "admin", locals: {reserved_books: @reserved_books, waitlist: @waitlist, requests: @requests} %>
<% else %>
  <%= render partial: "student", locals: {waitlist: @student_waitlist} %>
<% end %>
